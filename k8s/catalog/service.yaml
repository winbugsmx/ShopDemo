apiVersion: v1
kind: Service
metadata:
  name: shopdemo-catalog
  namespace: shopdemo
spec:
  type: LoadBalancer
  selector:
    app: shopdemo-catalog
  ports:
    - port: 8080
      targetPort: 8080
