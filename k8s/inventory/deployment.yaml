apiVersion: apps/v1
kind: Deployment
metadata:
  name: shopdemo-inventory
  namespace: shopdemo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: shopdemo-inventory
  template:
    metadata:
      labels:
        app: shopdemo-inventory
    spec:
      containers:
        - name: inventory-api
          image: shopdemo-inventory:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          env:
            - name: ASPNETCORE_ENVIRONMENT
              value: Production
            - name: ConnectionStrings__DefaultConnection
              valueFrom:
                secretKeyRef:
                  name: shopdemo-secrets
                  key: PG_INVENTORY_CONN
            - name: EventHubs__Enabled
              value: "true"
            - name: EventHubs__ConnectionString
              valueFrom:
                secretKeyRef:
                  name: shopdemo-secrets
                  key: EVENT_HUBS_CONNECTION_STRING
            - name: EventHubs__EventHubName
              value: shopdemo-events
            - name: EventHubs__ConsumerGroup
              value: inventory-service
            - name: EventHubs__CheckpointStorageConnectionString
              valueFrom:
                secretKeyRef:
                  name: shopdemo-secrets
                  key: AZURITE_CHECKPOINT_CONN
            - name: EventHubs__CheckpointContainerName
              value: inventory-checkpoints
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /alive
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 15
            failureThreshold: 3
