---
description: Implement from a ShopDemo SPEC path
argument-hint: [spec path e.g. specs/01-catalog/SPEC.md]
---

Implement: $ARGUMENTS

1. Read the SPEC and all linked docs
2. List acceptance criteria as checklist
3. Implement only what the SPEC requires
4. Build and suggest verification

Azure/AWS: use deploy commands only when SPEC mentions deployment.

Spanish replies to user.
