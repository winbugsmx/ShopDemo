---
description: AWS deployment (ECS, EKS, ECR) — use only for AWS tasks
globs: Documentación del Proyecto/despliegue/aws/**/*
alwaysApply: false
---

# AWS deploy

- Spec: `spec-driven/specs/07-deploy-aws/SPEC.md`
- Guides: `Documentación del Proyecto/despliegue/aws/`, `Documentación del Proyecto/despliegue/eks/`, `Documentación del Proyecto/integracion-ia/IMPLEMENTACION-DESPLIEGUE-MCP-AWS.md`
- Use AWS Console and `aws` / `eksctl` from implementation docs.
- ECS services: `shopdemo-*`. MCP: `shopdemo-mcp`.
- Event Hubs remains on Azure — ensure outbound HTTPS from VPC.
- Do not use `az` CLI in this context.
